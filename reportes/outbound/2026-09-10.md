[OUTBOUND · 2026-09-10 12:19 Madrid]

KPI:
| dato | valor | vs. anterior |
|---|---|---|
| Respuestas campaña 3789100 | sin dato (falta smartlead_key) | primer reporte |
| Interesados / semana | sin dato | — |
| Tratos creados en Pipedrive (pipeline 6) | 0 (falta pipedrive_key) | — |
| Rebotes | sin dato | — |
| Leads cargados pendientes de envío | sin dato | — |

Hecho hoy:
- `git pull` de la rama: al día.
- `scripts/bootstrap_credenciales.py`: credenciales disponibles = ninguna. `~/.outbound/` existe pero está vacío y no hay variables de entorno.
- No se han leído ni clasificado las 20 respuestas de la campaña 3789100 ni se ha tocado Pipedrive: sin claves no hay acceso.

Hallazgos:
- Bloqueo total del ciclo diario. Las 20 respuestas pendientes en Smartlead siguen sin clasificar y cualquier Interesado entre ellas no tiene todavía trato ni ha llegado a David (objetivo: < 1 hora).

Propongo:
- Maikel: añadir hoy `SMARTLEAD_KEY` y `PIPEDRIVE_KEY` como variables de entorno del entorno «Equipzilla» en claude.ai/code (o los ficheros `~/.outbound/smartlead_key` y `~/.outbound/pipedrive_key`). En cuanto estén, relanzo el arranque: clasificación de las 20 respuestas, categoría aplicada en Smartlead, trato en pipeline 6 con nota y propietario David para cada Interesado, y lista de Interesados (nombre, empresa, teléfono, qué pide, máquina de `data/machines.json` que encaja) en este mismo reporte.
- Mientras tanto, David puede revisar la bandeja de respuestas directamente en Smartlead si hay urgencia.

Necesito:
- `smartlead_key` (imprescindible para leer y categorizar respuestas).
- `pipedrive_key` (imprescindible para comprobar/crear tratos en pipeline 6).
- Opcional para el resto del ciclo: `apify_key` (listas nuevas con `captar_leads.py`).
