[CRM / DATOS · 2026-09-11 14:16 Madrid · ciclo diario + informe del piloto (viernes)]

KPI: segundo día sin acceso a Pipedrive ni a la hoja de stock desde esta sesión. Las cinco métricas del agente siguen en n/d.

| dato | valor | vs. anterior |
|---|---|---|
| Leads sin dueño (pipelines 6 y 16) | n/d | igual (10/09) |
| Tratos sin actividad > 7 d | n/d | igual |
| Tiempo medio a primer contacto | n/d | igual |
| Perdidos sin motivo (90 d) | n/d | igual |
| Demanda no servida (nº · valor) | n/d | igual |
| Credenciales en esta sesión | 0 de 10 | igual |

Hecho hoy:
- Rutina de las 14:00: pull, bootstrap («credenciales disponibles: ninguna»), relectura de la ficha. Sin variables de entorno ni ficheros en ~/.outbound/.
- Ciclo de integridad NO ejecutado contra Pipedrive. No se inventan cifras ni IDs.
- Leídos los reportes de hoy de Ads, Outbound, Seguimiento y SEO y el cierre de Analítica de ayer. Ningún bug del CRM visual reportado; nadie de Maikel, Andrés o Lorenzo ha dejado indicaciones en esta sesión.
- Viernes: bloque del informe del piloto para Lorenzo redactado a partir del código del repo (abajo). Las frecuencias de uso reales quedan pendientes de acceso a Pipedrive.

Hallazgos:
1. **Las credenciales existen, pero no en esta sesión.** El commit 24c7e16 (hoy 08:13 Madrid, sesión del Director de Growth session_01VocQvf…) actualizó `data/stock_visto.json` tras ejecutar `scripts/aviso_stock_leads.py`, que lee la hoja de stock de David con la cuenta de servicio de Google y cruza con la «Cola comercial». Es decir, la sesión del Director sí tiene al menos `google_sa.json`, y los avisos de stock se están generando. El bloqueo de los 7 agentes es de configuración del entorno «Equipzilla» de cada sesión, no de falta de claves en la empresa.
2. **Campo usado y no registrado en el contrato.** `scripts/cola_comercial.py` lee el campo de Pipedrive «Precio maximo» además de los seis registrados en `docs/crm-os/01-arquitectura-piloto.md` (Gclid, Utm, assetType, assetUse, Razón Social, profesional). Hay que añadirlo a la tabla del contrato; no es un campo nuevo, ya existe en Pipedrive.
3. **Precio de máquinas: la fuente es la hoja de David, no `data/machines.json`.** Analítica documentó ayer que la Hyster H2.5FT 2019 figura a 15.000 € en machines.json y a 14.000 / 13.000 € en la hoja (el 15.000 € es la unidad de 2021). Afecta al CRM visual si en algún punto pinta precios desde machines.json. Sin credenciales no puedo verificar hoy qué fuente usa `?solo=stock` en producción; queda como primera comprobación cuando haya acceso.

Propongo:
- Maikel: copiar al entorno «Equipzilla» las variables que ya tiene la sesión del Director (`PIPEDRIVE_KEY`, `GOOGLE_SA_JSON`, y si existen `BREVO_KEY` y `SMARTLEAD_KEY`). Es el mismo bloqueo que ayer, pero hoy sabemos que las claves están operativas en otra sesión. Antes del lunes 14/09 a las 14:00.
- Director de Growth: mientras no se copien, ejecutar desde su sesión `python3 scripts/cola_comercial.py 60 --sheet` una vez al día para que David y Héctor tengan cola actualizada; el informe de integridad con IDs lo hago yo en cuanto tenga clave.
- Lorenzo: revisar el bloque del piloto de abajo y confirmar si se añade «Precio maximo» a la tabla de campos del contrato (`docs/crm-os/01-arquitectura-piloto.md`, sección 3). Lo edito yo con su OK.

Necesito:
- `PIPEDRIVE_KEY` y `GOOGLE_SA_JSON` en el entorno de esta sesión. Sin ellas, el lunes será el tercer ciclo sin integridad.

---

## Informe del piloto para Lorenzo · semana 1 (7-11 sep) · versión sin datos de uso

Contexto: sin acceso a Pipedrive esta semana desde este agente, el bloque recoge lo que el código del piloto usa realmente (fuente: repo, rama claude/brevo-equipzilla-template-t47ip0). Las frecuencias de uso por campo se añadirán la primera semana con acceso.

**1. Entidades de Pipedrive que toca el piloto**
- Tratos (`/deals`, `/deals/{id}`, `/deals/{id}/flow` para historial de etapas), pipelines 6 y 16.
- Personas (`/persons`, `/persons/search` para deduplicar por email y teléfono).
- Etapas (`/stages`) y definición de campos (`/dealFields`, `/personFields`, resueltos por nombre en tiempo de ejecución, no por hash).
- Actividades (`/activities`, lectura para «última actividad»).
- Notas (`/notes`, única escritura habitual del agente, firmada «desde el CRM visual» / «agente»).
- Leads (`/leads/{id}`, lectura puntual).

**2. Campos personalizados que lee el código**
- Registrados en el contrato: Gclid, Utm, assetType, assetUse, Razón Social, profesional.
- Usado y NO registrado: **Precio maximo** (lo lee `cola_comercial.py` para el cruce con stock). Propuesta: añadirlo a la tabla del contrato.
- Derivados que no se persisten (se recalculan cada hora): score, prioridad, canal de entrada, tipo de empresa, días hasta oferta, «qué tenemos que encaja».

**3. Escrituras del piloto en Pipedrive (todas con autor, vía API del CRM visual, `docs/crm-api.md`)**
- Nota en el trato (además se copia en la pestaña «CRM · notas» como caché).
- Cambio de etapa dentro del pipeline 6 (45 Lead recibido · 33 Enviar oferta · 37 Oferta enviada · 38 Aceptada · 28 Alquilador asignado · 46 Entrega).
- Marcar perdido con motivo.
No hay ninguna otra escritura. No se crean campos, personas ni tratos desde el piloto (los tratos nuevos los crea la web o el agente Outbound por su propio flujo).

**4. Vistas derivadas en producción**
- Sheet 1wyWmrmg…: «Cola comercial», «Panel · en vivo», «Leads · entrantes», «CRM · notas» (esta última, caché documentada de notas ya en Pipedrive).
- `GET https://ocasion.equipzilla.com/api/crm` con clave de API y filtro `?solo=` (stock, cola, notas, tiempos). Comprobado hoy: el CRM visual responde 200 y la API devuelve 401 sin clave, como debe.
- Ficheros de idempotencia en `data/`: `stock_visto.json` (actualizado hoy 08:13 por la rutina del Director) y `ads_offline_visto.json`.

**5. Procesos que han funcionado esta semana (con la evidencia disponible)**
- Aviso de stock → leads que encajan: ejecutándose a diario a las 8:00 desde la sesión del Director (evidencia: commits diarios de `stock_visto.json` 10/09 y 11/09). Cifras de leads avisados: n/d desde esta sesión.
- Cola comercial, integridad, tiempo a primer contacto y demanda no servida: NO ejecutados esta semana por este agente (sin credenciales). Sin cifras.

**6. Qué habría que persistir (propuesta preliminar, a validar con datos)**
- Canal de entrada del trato (hoy derivado de Gclid/Utm/origen; es el candidato más claro a campo del modelo definitivo).
- Referencia de la máquina ofertada, ligada al futuro endpoint de stock de tecnología (hoy es texto libre en el trato y se casa con la hoja por modelo, año y precio).
- Motivo de pérdida normalizado (Pipedrive ya lo tiene; falta comprobar que se rellena).

**7. Integraciones necesarias (sentido del flujo)**
- Pipedrive → piloto: lectura de tratos, personas, etapas, actividades, notas, flow.
- Piloto → Pipedrive: notas, cambio de etapa, perdido con motivo.
- Hoja de stock de David → piloto (lectura). Pendiente: endpoint de stock de tecnología para retirarla.
- Brevo y Smartlead → piloto (señales de clic/respuesta, solo lectura).
- Pipedrive → Google Ads (conversiones offline por gclid).

**8. Pendiente de tecnología (sin cambios respecto al contrato)**
- Endpoint de lectura de stock real.
- Formularios web enviando gclid y utm al crear el trato.
- OK a la tabla de fuentes de verdad y, ahora, al campo «Precio maximo» en la tabla de campos.
