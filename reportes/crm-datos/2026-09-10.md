[CRM / DATOS · 2026-09-10 12:20 Madrid]

KPI: sin datos. No se ha podido leer Pipedrive ni la hoja de stock.

| dato | valor | vs. anterior |
|---|---|---|
| Leads sin dueño (pipelines 6 y 16) | n/d | primer turno |
| Tratos sin actividad > 7 d | n/d | primer turno |
| Tiempo medio a primer contacto (48 h) | n/d | primer turno |
| Perdidos sin motivo (90 d) | n/d | primer turno |
| Demanda no servida (nº · valor) | n/d | primer turno |

Hecho hoy:
- Pull de la rama, bootstrap de credenciales y lectura de reglas, ficha y contrato con tecnología.
- El bootstrap devuelve «credenciales disponibles: ninguna». No hay ficheros en ~/.outbound/ ni variables de entorno en el entorno «Equipzilla».
- No se ha ejecutado ningún script contra Pipedrive ni Sheets: sin clave no hay informe de integridad y no se inventan cifras.

Hallazgos: nada relevante (sin acceso a datos no hay hallazgos).

Propongo:
- Maikel: añadir hoy en claude.ai/code, entorno «Equipzilla», las variables `PIPEDRIVE_KEY` (token de API de Pipedrive) y `GOOGLE_SA_JSON` (contenido del JSON de la cuenta de servicio con lectura de la hoja de stock de David). Son las dos imprescindibles para este agente.
- Opcionales para el ciclo completo de la cola comercial: `BREVO_KEY` (clickers) y `SMARTLEAD_KEY` (señales de frío). Sin ellas la cola se calcula solo con Pipedrive + stock.
- En cuanto estén, relanzar el arranque de este agente: informe de integridad (pipelines 6 y 16) con IDs de trato, tiempo a primer contacto de las últimas 48 h y tabla de demanda no servida.

Necesito:
- `PIPEDRIVE_KEY` y `GOOGLE_SA_JSON` en el entorno «Equipzilla». Bloqueo total hasta entonces.
