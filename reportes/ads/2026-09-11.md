[ADS · 2026-09-11 08:25 Madrid]

KPI: sin datos de la cuenta. Segundo día sin credenciales de Google Ads ni Pipedrive.

| dato | valor | vs. anterior |
|---|---|---|
| Leads/día | — | — |
| CPA 7 d | — | — |
| Coste ayer / 7 d | — | — |
| % leads Ads → oferta (gclid en Pipedrive) | — | — |
| Negativas añadidas | 0 | 0 |
| Landings de campaña operativas (curl + título) | 4 de 4 | primer chequeo |

Hecho hoy: `git pull` (entran reportes de seguimiento y seo), bootstrap → «credenciales disponibles: ninguna» (`~/.outbound/` existe pero está vacío; sin variables de entorno). Ciclo diario NO ejecutado contra la API (ni métricas, ni términos, ni negativas, ni `ads_conversiones_offline.py`). Sin indicaciones de Maikel o Andrés en esta sesión. Como no dependía de la API, he auditado las 4 landings de las campañas y su etiquetado de conversiones.

Hallazgos (verificados con curl, sin cifras de la cuenta):
1. **La URL final de plataformas redirige.** `/compra/maquinaria/ocasion/plataforma-elevadora-segunda-mano` devuelve 308 → `/compra/maquinaria/usada/plataforma-elevadora-segunda-mano`. Las otras 3 (miniexcavadoras, excavadoras, dumpers) responden 200 directas con título correcto.
2. **Las landings no cargan la etiqueta AW-18345032067.** Solo cargan GTM-MF9NHGM. Ese contenedor lleva 2 conversiones de Google Ads de otra cuenta (778882628), el enlazador de conversiones y GA4 (G-8BYZBEPVRQ, G-NG45JRN3ZJ); no contiene ninguna referencia a AW-18345032067 ni al evento `lead_chat` que el widget empuja al dataLayer.
3. **Consecuencia probable para las 2 conversiones online:** «quiz enviado» se dispara desde `quiz/form.html` con gtag AW-18345032067, pero el quiz vive en `equipzilla-quiz.vercel.app` (otro dominio) y el código no pasa el gclid, así que la atribución al clic es dudosa. «chat contactado» (`quiz/widget.js`) envía a AW-18345032067 desde la landing, donde ese ID no está configurado: riesgo de que no se registre. No puedo confirmarlo sin ver la cuenta; con API lo compruebo en 5 minutos (conversiones últimos 30 d por acción).

Propongo:
- Maikel (hoy): variables de entorno en el entorno «Equipzilla»: `GOOGLE_SA_JSON`, `GOOGLEADS_DEV_TOKEN`, `PIPEDRIVE_KEY`. Con eso ejecuto el ciclo completo el mismo día.
- Yo, en cuanto haya API (no toca presupuesto ni puja): cambiar la URL final del grupo plataformas a la ruta `/usada/` para quitar la redirección; comprobar en la cuenta cuántas conversiones «quiz enviado» y «chat contactado» han entrado en 30 d.
- Lorenzo (esta semana): añadir en GTM-MF9NHGM la Google tag AW-18345032067 y una conversión con activador en el evento `lead_chat`; pasar el gclid al quiz (parámetro en la URL del iframe) o servirlo bajo equipzilla.com. Sin esto la puja «Maximizar conversiones» aprende de datos incompletos.
- Mejora ejecutada hoy: ninguna. No hay nada ejecutable sin API.

Necesito (bloqueo, igual que ayer):
- `google_sa.json` — cuenta de servicio para Google Ads API v22 (y Data Manager API para conversiones offline).
- `googleads_dev_token` — developer token de Google Ads.
- `pipedrive_key` — para cruzar gclid → oferta enviada / operación ganada.
