[ADS · 2026-09-10 12:19 Madrid]

KPI: sin datos. No se pudo consultar Google Ads ni Pipedrive (faltan credenciales).

| dato | valor | vs. anterior |
|---|---|---|
| Leads/día | — | — |
| CPA 7 d | — | — |
| Coste ayer / 7 d | — | — |
| % leads Ads → oferta (gclid en Pipedrive) | — | — |
| Negativas añadidas | 0 | — |

Hecho hoy: `git pull` (rama al día), `python3 scripts/bootstrap_credenciales.py` → «credenciales disponibles: ninguna». Ciclo diario NO ejecutado: sin acceso a la cuenta 3057448284 no hay métricas, términos de búsqueda, negativas ni subida de conversiones offline. No he inventado cifras.

Hallazgos: bloqueo total por credenciales. No existe `~/.outbound/` ni variables de entorno. Es el primer reporte de este agente (no hay reportes previos en `reportes/ads/`).

Propongo:
- Maikel: añadir hoy las variables de entorno del entorno «Equipzilla» en claude.ai/code (o los ficheros en `~/.outbound/`, chmod 600). En cuanto estén, relanzo el ciclo completo y entrego el reporte el mismo día.
- Mientras tanto, nada que ejecutar sin tocar presupuesto/puja que no requiera la API.

Necesito (bloqueo):
- `google_sa.json` — cuenta de servicio para Google Ads API v22 (y Data Manager API para conversiones offline).
- `googleads_dev_token` — developer token de Google Ads.
- `pipedrive_key` — para cruzar gclid → oferta enviada / operación ganada (`scripts/ads_conversiones_offline.py`).
